using UnityEngine;

[CreateAssetMenu(fileName = "ShopRoom", menuName = "RoomTypes/ShopRoom")]
public class ShopRoom : Room
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

}
